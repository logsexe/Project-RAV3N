[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fieldos"
version = "2.0.1"
description = "Minimal PySide6 FIELD//OS shell for RAVEN RVN-01"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.8,<7.0",
]

[project.scripts]
fieldos = "fieldos.qt_app:main"

[tool.setuptools.packages.find]
include = ["fieldos*"]
